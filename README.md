## Unwrapping everything:
1. Clone this repo into `~/.dotfiles/`
2. Install `devbox`
3. `cp ~/.dotfiles/.local/share/devbox/global/default/devbox.json ~/.local/share/devbox/global/default/devbox.json`
4. `cd ~/.dotfiles && make`
